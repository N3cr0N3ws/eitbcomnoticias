# Configuración general del sitio
title: Eitb Noticias
description: "Noticias con contexto."
url: "https://www.eitbnoticias.com" # URL para desarrollo local o producción
baseurl: "" # Vacío porque el sitio está en el root del dominio

# Directorios y exclusiones
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - node_modules
  - _posts
  - mergeJson.js
  - pages/

# Configuración de Jekyll
markdown: kramdown
data_dir: "_data"
incremental: true

# Configuración de Liquid para manejo estricto de variables
liquid:
  error_mode: strict
  strict_variables: true
  strict_filters: true